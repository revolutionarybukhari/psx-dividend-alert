# Optional: override the path to config.json
# PSX_ALERT_CONFIG=/etc/psx-dividend-alert/config.json

# Optional: pino log level — trace, debug, info, warn, error, fatal
# LOG_LEVEL=info

# Optional: NODE_ENV affects whether logs are pretty-printed
# NODE_ENV=production
